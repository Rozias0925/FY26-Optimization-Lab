# 從這裡開始

這個專案看起來有很多資料夾，是因為網站工具會自動產生套件與暫存檔。你真正需要認識的程式只有下面 6 個檔案。

## 你真正會改的 6 個檔案

| 想改什麼                                         | 檔案在哪裡                         |
| ------------------------------------------------ | ---------------------------------- |
| 整個實驗畫面、中文／英文文字、按鈕與段落         | `components/optimization-lab.tsx`  |
| 熱力圖、座標軸、縮放、動畫路徑與 loss curve      | `components/optimization-plot.tsx` |
| 三個 loss functions 與五個 optimizers 的數學算法 | `lib/optimization.ts`              |
| LaTeX 數學式如何顯示                             | `components/math.tsx`              |
| 網站整體顏色與共用外觀                           | `app/globals.css`                  |
| 瀏覽器頁籤標題與全站外框                         | `app/layout.tsx`                   |

如果只想修改首頁內容，先打開 `components/optimization-lab.tsx` 就好。

## 可以完全忽略的資料夾

- `node_modules/`：安裝套件時自動下載的程式庫。檔案很多，不要手動修改。
- `.vinext/`、`.next/`：執行網站時自動產生的快取。關掉或重開網站都可能改變。
- `dist/`：正式建置時自動產生的輸出，不是原始碼。
- `.git/`：Git 用來記錄版本歷史的隱藏資料，不要手動修改。
- `.openai/`：未來發佈網站時使用的設定，目前不需要碰。
- `components/ui/`：現成的 Button、Input、Tabs 等底層 UI 積木。除非要重做元件本身，否則不用看。

這些「奇怪檔案」不是你的資料，也不是我偷偷加入的內容；它們大多是 pnpm、Vinext、Vite 或 Git 維持網站運作所需的自動檔案。

## 其他設定檔，也先不用改

- `package.json`：專案使用哪些套件、有哪些執行指令。
- `pnpm-lock.yaml`：記錄套件的精確版本，通常非常長。
- `tsconfig.json`：TypeScript 規則。
- `vite.config.ts`、`next.config.ts`：網站開發工具的設定。
- `postcss.config.mjs`、`components.json`：CSS 與 shadcn UI 的設定。

## 最簡單的閱讀順序

1. 先開 `components/optimization-lab.tsx` 看畫面與文字。
2. 想懂圖怎麼畫，再看 `components/optimization-plot.tsx`。
3. 想懂 optimizer 怎麼算，最後看 `lib/optimization.ts`。

其餘檔案現在都可以先忽略。
